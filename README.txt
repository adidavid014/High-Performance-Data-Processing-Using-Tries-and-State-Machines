To compile both standard and optimized executables, run "make all"
The additional argument when running the executable is the name of the text file.
There are no known issues.
Output is in a text file named [inputfile].txt-results
There are no memory leaks in either implementation. 